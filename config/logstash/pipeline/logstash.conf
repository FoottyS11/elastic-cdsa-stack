input {
  tcp {
    port => 5000
    codec => json
    type => "json_event"
  }
}

filter {
  # Add filters for EVTX parsing if needed
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-evtx-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
